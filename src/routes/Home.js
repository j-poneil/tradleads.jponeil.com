import React from 'react'
import styled from 'styled-components';
import { device } from '../breakpoints/breakpoints';

export const Home = () => {
    /*
        TODO - Add some content!
            * The about page content would be OK here... but maybe a photo and some text would be more appropriate?
    */
    return (
        <div>
            Home...
        </div>
    )
}
